import { Reporter, Suite, FullConfig, TestCase, TestError, FullResult } from '@playwright/test/reporter';
import { ActionType, ITeamcityReporterConfiguration } from './teamcity.model';
declare class TeamcityReporter implements Reporter {
    #private;
    private configuration?;
    flowId: string;
    rootSuite: Suite;
    constructor(configuration?: ITeamcityReporterConfiguration | undefined);
    onBegin(config: FullConfig, suite: Suite): void;
    onError(error: TestError): void;
    onTestBegin(test: TestCase): void;
    onEnd(result: FullResult): void;
    logToTC(action: ActionType, parts: string[]): void;
    static escape(text: string): string;
}
export default TeamcityReporter;
//# sourceMappingURL=teamcity.reporter.d.ts.map